@using Application.DTOs
@inject IJSRuntime js
@rendermode InteractiveServer

@page "/"

@inject BlazorServer.Services.CategoryService CategoryService

<button @onclick="CallJavaScriptFunction">Chamar função JavaScript</button>

@* Implementado javascript, agora passar tudo para esse projeto que esta no outro, até o layout, cdn bootstrap ... *@
<PageTitle>Home</PageTitle>

<h1>DOM Cleanup Example</h1>

<div id="cleanupDiv"></div>


<h3>Categorias</h3>

@if (_categories == null)
{
    <h3>Carregando...</h3>
}
else
{

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Descrição</th>
                <th scope="col">Editar</th>
                <th scope="col">Deletar</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in _categories)
            {
                <tr>
                    <td>@category.Name</td>
                    <td>
                        <a href="edit">
                            <svg style="color:blue;" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pencil-fill" viewBox="0 0 16 16">
                                <path d="M12.854.146a.5.5 0 0 0-.707 0L10.5 1.793 14.207 5.5l1.647-1.646a.5.5 0 0 0 0-.708l-3-3zm.646 6.061L9.793 2.5 3.293 9H3.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.207l6.5-6.5zm-7.468 7.468A.5.5 0 0 1 6 13.5V13h-.5a.5.5 0 0 1-.5-.5V12h-.5a.5.5 0 0 1-.5-.5V11h-.5a.5.5 0 0 1-.5-.5V10h-.5a.499.499 0 0 1-.175-.032l-.179.178a.5.5 0 0 0-.11.168l-2 5a.5.5 0 0 0 .65.65l5-2a.5.5 0 0 0 .168-.11l.178-.178z" />
                            </svg>
                        </a>
                    </td>
                    <td>
                        <svg style="color:red;cursor:pointer" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-x-lg" viewBox="0 0 16 16">
                            <path d="M2.146 2.854a.5.5 0 1 1 .708-.708L8 7.293l5.146-5.147a.5.5 0 0 1 .708.708L8.707 8l5.147 5.146a.5.5 0 0 1-.708.708L8 8.707l-5.146 5.147a.5.5 0 0 1-.708-.708L7.293 8z" />
                        </svg>
                    </td>
                </tr>

            }
        </tbody>
    </table>

}



@code {

    private List<CategoryDTO>? _categories;

    protected override async Task OnInitializedAsync()
    {
        // await js.InvokeVoidAsync("alert", "OnAfterRender");
        _categories = (await CategoryService.GetAllCategoriesAsync()).ToList();
    }


    private async Task CallJavaScriptFunction()
    {
        // await JSRuntime.InvokeVoidAsync("nomeDaFuncaoJavaScript");
        await js.InvokeVoidAsync("alert", "Testando javascript");
    }



    // protected override async Task OnAfterRenderAsync(bool firstRender)
    // {

    //     await js.InvokeVoidAsync("alert", "OnAfterRender");

    // }

}